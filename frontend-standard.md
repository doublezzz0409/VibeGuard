```markdown
---
name: frontend-standard
description: 前端契约驱动实现。严格按功能需求说明施工，分步机械执行，强制交叉校验，内置状态覆盖与E2E可达性验证。
---

# 🔴【最高优先级·强制执行模式】

## 设计哲学（给执行 AI 看，理解你为什么必须这样做）
你是一个没有全局视野的执行单元。你能写出单个文件的高质量代码，但你天生不会主动检查：
- 你写的链接是否指向了不存在的路由
- 你调用的 API 是否和后端约定的字段名一致
- 你引用的类型是否在 types.ts 中已定义
- 你写的组件是否覆盖了 Loading/Empty/Error 状态
- 你定义的字段格式是否存在“格式不限”这类假宽容表述
- 你产出的文档是否存在前后矛盾或模糊空间
- 你产出的 mock-api-doc 是否覆盖了项目的全部接口，而非仅本次变更的部分
- 你产出的新实体名、字段名、API 端点是否与项目已有命名冲突
- 你产出的 mock-api-doc 是否包含了后端可直接引用的数据库字段名、数据库类型和表名，且这些信息是否与 naming-convention.md 完全一致

因此，本 command 通过“契约层”和“强制交叉校验”来弥补你的天生缺陷。每一个“交割步骤”都要求你把产出反向对照到已有的契约文件，并确保没有任何字段存在格式歧义，任何命名存在冲突，且后端信息完整可追溯。
- **不信任你的自觉，只信任你的对照结果。**
- **不确定的格式必须标记为待确认，禁止用“格式不限”掩盖。**
- **mock-api-doc 是项目全局接口契约的唯一真相源，每次必须全量输出，且自检重复内容。**
- **naming-convention.md 是项目命名的唯一规则源。步骤0/1 必须维护它，步骤14 必须确认它，并基于它生成后端转换信息。**

## 铁律（违反立即报错并重新执行）
1. 我下面写的每一条步骤，你必须**100%机械执行**，一条都不能漏。
2. 禁止自作聪明、禁止优化、禁止合并步骤、禁止省略任何细节。
3. 你无权判断步骤是否必要，**只准执行，不准思考**。
4. 每完成一步，必须输出：✅步骤X 完成。
5. 任何步骤中如果产出与已有契约不一致，必须立即报告“契约冲突”，并停止后续步骤，等待用户裁决。禁止自行选择保留哪一方。
6. 任何字段的类型、格式、空值语义必须精确到可判定真伪的程度。禁止使用“格式不限”“任意”“可协商”“视情况而定”等模糊表述。做不到精确就标记 `// TODO: 待确认`。
7. mock-api-doc.md 是项目的全局接口契约文件，无论项目是新建还是增量开发，每次执行步骤14 都必须生成覆盖项目当前全部 API、全部数据模型、全部字段格式约定的完整文档。禁止只输出本次变更的差异部分。
8. 步骤14 完成后，必须在对话中输出复盘日志（含步骤完成清单、生成模式、全局覆盖统计、模糊表述扫描结果、命名规范同步确认、后端信息生成确认），并将此日志以 HTML 注释形式追加写入 mock-api-doc.md 文件末尾。
9. naming-convention.md 是项目命名的唯一规则源。步骤0/1 结束时必须生成或更新它，步骤14 结束时必须确认它已与 types.ts 同步。任何新增的实体名、字段名、ID 前缀、API 端点必须在 naming-convention.md 的查重目录中无冲突。
10. **所有产出文件的写入路径必须严格按照本文件约定的路径，禁止自行判断或创建新目录。**

## 你的指令（全栈开发全流程强制执行）

### 第 0 阶段：前置条件检查（强制执行）

步骤0：验证当前工作目录及执行本命令所需的前置输入是否存在。

**检查清单：**

1. **工作目录验证**：获取当前工作目录，检查当前目录下是否存在 **`blog-frontend/` 子目录**。若不存在，输出以下错误并中止执行：

```
❌ 错误：当前工作目录不是项目根目录。
请 cd 到包含 blog-frontend/ 和 backend/ 的目录后重新执行本命令。
```

若存在，输出确认信息：”✅ 工作目录验证通过 — 当前为项目根目录”。

2. 读取项目根目录（当前工作目录）下是否存在由 `/translate-requirement` 产出的 **功能需求说明**（通常为 Markdown 格式，内容包含”需求说明：[一句话概括]”等章节）。**该文件必须存在**，否则输出错误并中止。
3. 如果功能需求说明不存在，输出以下信息并停止：

```
❌ 前置条件不满足：未找到功能需求说明文件。
请先执行 `/translate-requirement` 完成需求精炼，获得功能需求说明后再执行本命令。
```

4. 若以上检查全部通过，输出”✅步骤0 完成 — 工作目录验证通过，功能需求说明已加载”。

### 步骤0.1：前端 .gitignore 维护

检查 `blog-frontend/.gitignore` 是否存在且包含以下必要条目。若不存在则创建，若缺少则追加。

**必须包含的条目：**
```
# 依赖
node_modules/

# 构建产物
dist/
dist-ssr/
*.local

# MSW 生成的文件
public/mockServiceWorker.js

# 环境变量文件（含敏感信息）
.env.local
.env.*.local

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
```

**执行动作：**
- 若文件不存在 → 创建 `blog-frontend/.gitignore` 并写入上述内容。
- 若已存在但缺少某些条目 → 追加缺失的条目。
- 输出确认信息：”✅步骤0.1 完成 — .gitignore 已就绪”。

---

### 第一阶段：数据与接口清算（前后端共同契约）

步骤1：从功能需求说明中提取所有数据实体（例如用户、列表项、表单字段），为每个实体定义 TypeScript interface。明确每个字段的名称、类型（string / number / boolean / 枚举值）、是否可选。将所有 interface 写入 `blog-frontend/src/types.ts` 文件。你的输出必须是完整的 `blog-frontend/src/types.ts` 内容，并在末尾注明”✅步骤1 完成”。

步骤1 补充约束（字段格式精确性强制规则）：

a) **类型精度**
- 每个字段的类型必须是精确的，禁止使用 `any`、`unknown`。

b) **ID 字段**
- 必须明确是 number 还是 string。
- 如果是 string，需进一步明确格式规则（UUID / 自增数字字符串 / 带前缀如 `"art-{uuid}"` / 其他）。
- 所有实体的 ID 格式必须在本步骤确定并统一，形成项目的“ID 命名规范”。同一实体家族（如 `Article` 和 `ArticleDetail`）的 ID 格式必须一致。
- 示例规范：文章=`"art-{id}"`，分类=`"cat-{id}"`，标签=`"tag-{id}"`，用户=`"user-{id}"`，评论=`"cmt-{id}"`。

c) **可空字段**
- 必须明确是 `null` 还是空字符串 `""` 或空数组 `[]`，禁止“可为空字符串或 null”的双重可能。
- **统一原则：业务数据中的可空字符串字段默认使用空字符串 `""`，可空数组字段默认使用空数组 `[]`，可空对象字段使用 `null`。项目全领域保持一致。**
- **特殊说明：顶层响应壳的 `data` 字段不受此条限制，其失败时可为 `null`。此条仅约束业务实体内部的字段。**

d) **日期/时间字段**
- 必须指定格式（ISO 8601 UTC 含时区 `Z` / Unix timestamp（秒/毫秒） / 仅日期 `YYYY-MM-DD`）。
- 默认标准：所有日期时间字段统一使用 ISO 8601 UTC 格式，如 `"2026-04-30T12:00:00Z"`。

e) **枚举型字段**
- 必须列出全部可能的值（如 `status: "draft" | "published" | "archived"`），并将枚举字符串写入 JSDoc 注释。

f) **字符类字段的字符集约束**
- 对于 `slug` 等 URL 路径标识字段，必须明确允许的字符集和格式规则。默认规则：**仅允许小写字母、数字、连字符 `-`，以字母开头，长度 1-200。** 写入 JSDoc 注释。
- 对于 `token` 等安全凭证字段，必须明确其格式结构。默认规则：**JWT 格式（三段 Base64URL 字符串，以 `.` 分隔），不含 `Bearer ` 前缀。** 写入 JSDoc 注释。
- 禁止使用“URL 友好标识”等仅人类可读的模糊描述作为字段注释。

g) **数值字段的范围约束**
- 对于可能影响性能的数值字段（如分页大小 `pageSize`、站点设置 `postsPerPage`），必须明确上下限。默认规则：**正整数，范围 1-100。** 写入 JSDoc 注释。
- 对于计数类字段（如 `viewCount`、`articleCount`），必须明确为非负整数。

h) **全量/部分更新语义（关联步骤2）**
- 对于将在后续步骤中暴露为 `PUT` 接口的实体，必须在本步骤的 JSDoc 中明确更新语义：`PUT` 默认全量更新（请求体包含全部字段），若为部分更新需显式标注。

i) **待确认项处理**
- 任何无法在当下确定的格式，**必须在该字段的 JSDoc 注释中标记 `// TODO: 待与后端确认`**，并在此步骤末尾汇总所有待确认项，逐条列出字段路径和待确认内容。
- 禁止写“格式不限”“任意”“视情况而定”等表述来替代待确认标记。

**🔴 步骤1 命名规范维护（强制执行）：**

在输出“✅步骤1 完成”之前，必须执行以下操作：

**如果是首次创建（项目无 `./naming-convention.md`）：**
- 基于刚生成的 `blog-frontend/src/types.ts`，自动生成 `./naming-convention.md` 初版。
- 文件必须包含：通用命名原则、实体与字段命名规则、ID 前缀表、实体与字段查重目录、API 路径命名规则、已有端点目录、前后端命名转换规则（第6章）。
- 所有已有实体和字段必须写入查重目录。
- 输出文件完整内容，并标注“✅ naming-convention.md 首次生成”。

**如果是增量执行（项目已有 `./naming-convention.md`）：**
- 读取已有文件，与当前 `blog-frontend/src/types.ts` 做全量对比。
- **必须逐字段比对，不得仅比对实体名**。每个实体下的每个字段必须逐一核对。
- 新增的实体和字段：追加到查重目录对应位置。
- 修改的字段：更新查重目录中的条目。
- types.ts 中存在但查重目录中缺失的字段：立即补入目录。
- 查重目录中存在但 types.ts 中已删除的字段：立即从目录中移除。
- 规则部分（第1/2/4/6章）保持不变，除非 `blog-frontend/src/types.ts` 的 JSDoc 中新增了格式约束（如新字段类型引入了新的命名规则），则同步更新第6章映射表。
- 比对完成后，输出一份”命名规范同步确认清单”，列出新增/修改/删除的条目数量。
- 输出更新后的文件完整内容，并标注”✅ naming-convention.md 已同步更新”。

步骤1 交割产物清单：
- 完整的 `blog-frontend/src/types.ts` 文件内容
- 完整的 `./naming-convention.md` 文件内容（首次）或更新说明（增量）
- 末尾汇总：所有“待确认”字段清单（若无则写“无待确认项”）
- 末尾注明“✅步骤1 完成”

步骤2：基于功能需求说明和 `blog-frontend/src/types.ts`，列出页面涉及的所有 API 端点（HTTP 方法、路径、功能描述）。为每个端点定义请求体/查询参数的类型，以及完整的响应体类型（必须包含 `code`, `data`, `message` 外壳，`data` 必须引用 `blog-frontend/src/types.ts` 中的类型）。将所有 API 类型定义写入 `blog-frontend/src/api-contract.ts`。输出 `blog-frontend/src/api-contract.ts` 完整内容，并注明”✅步骤2 完成”。

**步骤2 补充约束：**
- 每个 API 的请求体/响应体类型必须完全引用步骤1中定义的 interface，不得在此步骤重新定义字段格式。
- 如果步骤1存在“待确认”项，本步骤生成的 api-contract 中对应的类型定义必须保留 `// TODO` 标记，不得自行填补。
- 对于写操作接口（POST/PUT/DELETE），需明确是全量更新还是部分更新。默认 RESTful 约定：`PUT` = 全量更新（请求体包含全量字段），`PATCH` = 部分更新。若使用 `PUT` 做部分更新，必须在注释中说明。
- **新增约束：所有 API 端点路径必须遵循 `naming-convention.md` 中第4章的路径命名规则，且不得与已有端点目录冲突。若发现冲突，立即报告“端点命名冲突”并停止。**

步骤3：基于 `blog-frontend/src/api-contract.ts`，为每个 API 编写封装好的服务函数（例如 `getUserProfile()`, `updateNickname()`），函数必须明确参数类型和返回类型，不允许直接在组件中调用 `fetch`/`axios`。将服务函数写入 `blog-frontend/src/services/index.ts`。输出 `blog-frontend/src/services/index.ts` 完整内容，并注明”✅步骤3 完成”。

**步骤3 补充约束：**
- 服务函数的参数和返回类型必须与 `blog-frontend/src/api-contract.ts` 严格一致，不允许放宽或收紧。
- 服务函数必须统一处理 `code !== 200` 的情况，将其作为错误抛出（抛出 `message` 字段内容）。
- 服务函数不得修改请求体或响应体的字段名或格式。

### 第二阶段：Mock 环境搭建

**本阶段目标**：建立前端开发的双模式数据源——独立开发时使用 MSW Mock，联调或部署时切换到真实后端 API。两种模式的切换必须通过环境变量控制，禁止手动注释代码。

步骤4：生成临时 Mock Handlers（严格遵循当前 `blog-frontend/src/api-contract.ts` 和 `blog-frontend/src/types.ts`）。

- 安装 MSW 依赖（`npm install msw --save-dev`）并初始化（`npx msw init public/ --save`，若目录不同须根据项目调整）。
- 创建 `blog-frontend/src/mocks/handlers.ts`，为 `blog-frontend/src/api-contract.ts` 中列出的**每一个** API 编写：
  - 至少一个成功响应的处理器（返回数据严格匹配 api-contract 类型）
  - 所有写操作（POST/PUT/DELETE）分别编写至少一个典型失败响应（400 业务错误 + 401 未授权 + 500 服务器错误），每个失败响应必须标注其业务含义
- **临时 Mock 数据一致性规则**：由于此时 `mock-api-doc.md` 尚未生成（将在步骤14产生），临时 handler 基于当前 types.ts 和 api-contract.ts 生成。步骤14.5 将基于最终文档重新生成 handlers，因此本阶段允许临时偏差，但必须标注“临时版本”。
- 输出 `blog-frontend/src/mocks/handlers.ts` 完整内容，并注明”✅步骤4 完成 — 临时 Mock Handlers”。

步骤5：创建 `blog-frontend/src/mocks/browser.ts`，使用 `setupWorker` 导入 handlers。在应用入口文件 `blog-frontend/src/main.tsx` 中根据环境变量决定是否启动 MSW：

- **环境变量约定**：使用 `VITE_ENABLE_MOCK` 控制 Mock 的启用。
  - `VITE_ENABLE_MOCK` 为 `true` 或未定义（默认）→ 启动 MSW，拦截所有 API 请求。
  - `VITE_ENABLE_MOCK` 显式为 `false` → 不启动 MSW，请求直接到达 Vite proxy 或真实后端。
- **`.env.local` 推荐设置（非必须）**：开发者可将以下内容写入 `blog-frontend/.env.local` 以备联调时快速切换：
  ```
  # 默认启用 MSW，无需显式设置
  # 需要对接后端时，取消注释下一行：
  # VITE_ENABLE_MOCK=false
  ```
- **MSW 与 Vite proxy 的优先级**：MSW（Service Worker）在浏览器线程内拦截请求，优先级高于 Vite proxy。当 `VITE_ENABLE_MOCK` 未定义或显式为 `true` 时，所有匹配 handler 的请求将**不会**到达 Vite proxy。只有在 MSW 禁用时（`VITE_ENABLE_MOCK=false`），Vite proxy 才会生效。因此，`vite.config.ts` 中的 proxy 配置必须**始终指向真实后端地址**，不得与 MSW 的路径产生二义性。
- **入口文件条件启动逻辑**：
  ```ts
  if (import.meta.env.VITE_ENABLE_MOCK !== 'false') {
    const { worker } = await import('./mocks/browser');
    worker.start();
  }
  ```
输出 `blog-frontend/src/mocks/browser.ts` 内容、`.env.local` 内容，并确认 Mock 启用条件。标注 “✅步骤5 完成”。

### 步骤5.5：联调模式切换机制

本步骤确保前后端联调时数据来源可无歧义切换，且 Mock 数据与真实后端契约保持一致。

**动作清单（必须逐项完成）：**

1. 确认 `blog-frontend/mock-api-doc.md` 将在步骤14生成。本阶段跳过依赖它的检查。

2. **环境变量切换验证**：
   - 联调时，开发者将 `blog-frontend/.env.local` 中的 `VITE_ENABLE_MOCK` 设为 `false`，重启开发服务器。
   - 检查所有 API 请求是否通过 Vite proxy 或直接到达真实后端。
   - 如果后端尚未完成，保持 `VITE_ENABLE_MOCK=true`。

3. **启动脚本的协作关系**：
   - 项目根目录下的 `blog-frontend/start.bat`（或 `npm run dev`）仅负责启动前端开发服务器，与后端无关。
   - 后端有其独立的启动脚本（如 `backend/start.bat` 或 `uvicorn` 命令）。
   - 联调时，需**同时运行**前端和后端脚本，并确保 `VITE_ENABLE_MOCK=false`。
   - 当两个服务同时运行时，前端请求通过 Vite proxy 转发到后端（需在 `blog-frontend/vite.config.ts` 中配置 proxy 指向后端地址）。

4. **Vite proxy 配置检查**：
   - 必须确认 `blog-frontend/vite.config.ts` 中包含类似以下配置，且目标地址与后端实际地址一致：
     ```ts
     server: {
       proxy: {
         '/api': 'http://localhost:8000'
       }
     }
     ```
   - 该配置仅在 `VITE_ENABLE_MOCK` 显式为 `false` 时真正生效；若该环境变量未定义，MSW 默认启用，请求不会到达 Vite proxy。

5. **环境变量名称迁移**：
   - 本规范统一使用 `VITE_ENABLE_MOCK` 作为 MSW 开关的环境变量名。如果旧项目使用了其他变量名（如 `VITE_USE_MOCK`），必须统一迁移到 `VITE_ENABLE_MOCK`。
   - 迁移范围：`blog-frontend/.env.local`、`blog-frontend/.env.development`、`blog-frontend/.env.production`、`blog-frontend/src/main.tsx`（或入口文件）中所有对旧变量名的引用。

完成后输出联调模式配置说明，并标注 “✅步骤5.5 完成”。

### 第三阶段：组件原子化开发

步骤6：所有组件文件生成后写入 `blog-frontend/src/components/` 目录。基于功能需求说明中的“前端交互要点”和“交互流程”，画出组件树（父组件与子组件的层级关系），列出每个组件的名称、职责（只允许 1-2 个职责），并明确哪些组件有内部状态、哪些完全受控。输出组件树（文字描述或简图）和职责说明，标注“✅步骤6 完成”。

步骤7：为步骤6中列出的每一个组件定义 Props 接口（输入只通过 Props）。若组件需要异步数据，则定义其内部状态枚举：`loading | empty | error | ready`。先输出所有组件的签名草案（如 `function NicknameDisplay(props: { nickname: string; onEdit: () => void })`），等待用户确认名称和职责后再继续。确认后立即单个逐一实现，不能跳步。每实现完一个组件，输出该组件的完整代码，并标注“✅步骤7-X 完成”。所有组件实现完毕后，标注“✅步骤7 完成”。

步骤8：逐个检查所有含异步数据组件的四种状态实现：
- Loading 态（骨架屏或转圈，不得完全空白）
- Empty 态（占位提示，不得消失或报错）
- Error 态（错误信息+重试按钮，不得吞掉错误）
- Ready 态（正常数据展示）

若缺失任何一种状态，立即补齐。每补齐一个组件的状态，输出该组件的更新代码，并标注“✅步骤8-X 完成”。全部补完后，标注“✅步骤8 完成”。

### 第四阶段：路由与导航契约清算（弥补天生盲区）
> **设计意图：你在组件里写的每一个 `<Link>`、`useNavigate()`、`window.location` 都是一份“路由需求”。你必须证明这些需求已被路由表满足。这不是可选的检查，这是契约。**

步骤9：扫描项目中所有组件文件（`.tsx`、`.jsx`），提取所有路由跳转点：
- `<Link to="...">` 或 `<NavLink to="...">`
- `navigate("...")` 或 `history.push("...")`
- `window.location.href = "..."` 或 `router.push("...")`

整理成**路由需求清单**，每一条包含：跳转目标路径、所在文件及行号、跳转上下文（点击按钮/自动跳转/条件跳转等）。输出完整清单，标注“✅步骤9 完成”。

步骤10：读取路由注册文件（通常为 `App.tsx` 或独立的路由配置文件），提取所有已注册路由。将步骤9的路由需求清单与已注册路由做**全量对照**：
- 需求清单中的路径必须能在路由表中精确匹配或模式匹配（如 `/categories/:id` 匹配 `/categories/123`）
- 任何**未匹配的路径**必须立即处理，选项包括：
  a) 在路由表中注册对应路由
  b) 将链接改为已存在的路由（如改为带 query 参数的首页路径）
  c) 添加重定向规则

处理完毕后，输出对照表和处理结果，标注“✅步骤10 完成”。

### 第五阶段：组件交叉校验（防止割裂）

步骤11：执行以下交叉检查并输出结果：
1. 所有组件中 import 的类型、函数是否真实存在于 `blog-frontend/src/types.ts`、`blog-frontend/src/services/index.ts` 或对应组件文件中。
2. 所有组件中调用的 API 函数是否真实存在于 `blog-frontend/src/services/index.ts`。
3. 所有 services 函数中请求的 API 端点和请求/响应结构是否与 `blog-frontend/src/api-contract.ts` 一致。
4. 路由需求清单中所有路径对应的页面组件是否真实存在且被正确导出。
5. 所有组件中对 `blog-frontend/src/types.ts` 中字段的使用，是否与步骤1定义的格式约束一致。例如：若步骤1规定可空字符串字段为 `""` 而非 `null`，组件中的空值判断逻辑（如 `if (field)` vs `if (field !== null)`）必须对应正确。
6. 所有组件中使用的字段名、实体名，是否与 `naming-convention.md` 中查重目录的命名风格一致。例如：布尔字段是否使用了 `is`/`has` 前缀，计数类字段是否以 `Count` 结尾。若发现风格不一致，必须报告并修复。

若发现不一致，必须在继续前修复。全部通过后标注“✅步骤11 完成”。

### 第六阶段：双模冒烟验证
> **设计意图：测试必须包含“正向验证”和“逆向发现”两个动作。只测已注册路由是正向思维——你必须同时证明“不存在链接指向未注册路由”。**

步骤12：使用 Playwright（或用户指定的 E2E 框架）编写自动化测试脚本，分两部分：
**Part A：可达性测试（正向验证）**
- 遍历步骤10中确认的每一个已注册页面路由，访问并断言页面不为空白（至少有一个可见元素或特定标识文本）。
**Part B：全量链接扫描（逆向发现）**
- 基于步骤9的路由需求清单，逐一访问存在链接的源页面，点击或触发所有链接，断言跳转后页面不为空白、无 404 文本、无控制台路由错误。

输出测试脚本内容，并将脚本文件写入 `blog-frontend/e2e/` 目录，标注“✅步骤12 完成”。

步骤13：执行 E2E 测试的前置环境检查和执行。

**前置条件检查：**
- 确认开发服务器已启动（默认 `npm run dev` 或 `yarn dev` 在 `http://localhost:5173` 运行）。
- 确认 Playwright 浏览器已安装（`npx playwright install chromium`）。
- 若未满足以上条件，输出指引并**跳过自动化执行**，提示人工确认环境就绪后手动运行 `npx playwright test`。此时仍可输出测试脚本。

**若环境满足：**
- 启动项目（Mock 模式），运行步骤12的测试脚本。
- 测试必须 100% 通过。若失败，必须提供：
  - Playwright 详细错误日志
  - 网络请求快照（若有 API 调用失败）
  - 失败的链接路径及所在文件
- 然后最小化修复，重跑直到通过。

**无论是否自动执行，最终均输出环境检查结果和测试通过状态（或人工运行指引）。**

通过后标注“✅步骤13 完成”。

### 第七阶段：后端交付文档与 Mock 最终同步

步骤14：生成或更新 `blog-frontend/mock-api-doc.md`。此文件是**项目全局接口契约的唯一真相源**，面向后端开发人员，且必须包含后端可无歧义直接使用的数据库字段名、数据库类型和表名信息。

**🔴 全局完整性强制规则（最高优先级）：**

a) **项目范围确定**
- 在生成文档前，必须读取项目当前的 `blog-frontend/src/types.ts`、`blog-frontend/src/api-contract.ts` 和 `./naming-convention.md`，获取项目的**全部**数据实体、**全部** API 端点、**全部**命名规则和**全部**前后端转换映射。
- 文档必须覆盖当前项目的全部内容，而非仅本次开发新增或变更的部分。

b) **首次创建模式**
- 如果项目根目录不存在 `blog-frontend/mock-api-doc.md`，则：
  - 基于项目当前全部 `blog-frontend/src/types.ts` 和 `blog-frontend/src/api-contract.ts` 生成完整的全局文档。
  - 所有章节均为正式内容，无需标注“新增”。

c) **覆盖更新模式（🔴 强制两步执行）**
- 如果项目根目录已存在 `blog-frontend/mock-api-doc.md`，则必须严格按以下两步执行，不得合并：

  **第 1 步：差异分析（必须输出）**
  - 读取已有 `blog-frontend/mock-api-doc.md`，与当前 `blog-frontend/src/types.ts` 和 `blog-frontend/src/api-contract.ts` 做全量对比。
  - 输出一份**变更清单**，包含：
    - 🆕 新增的实体、接口、字段
    - 📝 修改的实体、接口、字段（含字段类型、约束变化）
    - ❌ 删除的实体、接口、字段
  - 将此清单先展示给用户确认。标注“变更清单已输出，等待确认”。
  - **用户确认或表示“继续”后**，才进入第 2 步。

  **第 2 步：覆盖生成标记文档**
  - 基于当前全部类型和契约，重新生成完整的 `blog-frontend/mock-api-doc.md`，覆盖旧文件。
  - 新文档中，所有变更必须使用以下标记：
    - 新增的章节/行/段落：在标题或单元格末尾追加 `🆕`
    - 修改的章节/行/段落：在标题或单元格末尾追加 `📝`
    - 未变更的内容保持原样，不得省略，不得添加任何标记。
  - 生成的文档必须覆盖全部实体和接口。

d) **绝对禁止的行为**
- 禁止生成一份仅包含“本次变更”或“本次新增接口”的差异文档。
- 禁止在增量开发时生成独立的新文档（如 `mock-api-doc-settings.md`）。
- 禁止省略任何项目已有的实体、接口或字段格式约定。
- 禁止在未输出变更清单前直接覆盖生成文档。

**必须包含的章节和内容（不可缺省）：**

a) **通用约定章节**
- 基础 URL
- 统一响应格式（含 `code`/`data`/`message` 字段说明和状态码约定）
- Content-Type 约定
- 认证方式（含 Token 格式说明：JWT 三段 Base64URL 以 `.` 分隔，不含 `Bearer ` 前缀，前端自行拼接）
- 分页参数约定（含 `page`/`pageSize` 参数说明，`pageSize` 上限值）
- 分页响应结构

b) **字段格式约定章节**（紧跟通用约定之后）
- 以表格形式统一列出所有字段类别的格式规则：ID、slug、可空字符串字段、可空数组字段、日期时间、数值、分页数值、布尔、Token **以及前后端命名转换速查表**。
- 前后端命名转换速查表必须包含：驼峰转蛇形示例、实体名转表名示例、ID前缀处理规则。内容严格截取自 `naming-convention.md` 第6章。
- 此章节不得出现“格式不限”“任意”“建议”“视情况而定”等模糊表述。

c) **数据模型定义章节**
- 项目当前**每一个**实体对应的数据模型表格，必须包含**五列**：字段（前端/API）、类型（TS）、数据库字段、数据库类型、约束。
- 数据库字段列的值必须由前端字段名通过 `naming-convention.md` 第6.1节规则机械转换得到（驼峰转蛇形）。
- 数据库类型列的值必须由前端类型通过 `naming-convention.md` 第6.4节默认映射表得到，若无匹配项则标记为 `⚠️待确认`。
- 约束列必须填写步骤1中定义的精确格式规则，同时将空值语义翻译为数据库的 `NOT NULL` / `DEFAULT ''` 等约束。
- 每个实体的标题旁必须用括号标注对应的数据库表名（由 `naming-convention.md` 第6.2节规则生成），格式为 `### ArticleDetail（文章详情） — 表: article_details`。
- 即使是增量开发，也必须列出项目**全部**实体，不可仅列本次涉及的实体。

d) **接口详细说明章节**
- 按公开/管理分组，项目当前**每一个**接口包含：HTTP 方法、Path、请求参数（含 Query/Body/Path，各参数的类型/必填/默认值/说明）、成功响应示例、典型失败响应示例、Mock 模拟行为说明
- 即使是增量开发，也必须列出项目**全部**接口，不可仅列本次涉及的接口

e) **Mock 分支汇总章节**
- 表格列出项目当前**所有**接口的已模拟成功/失败分支及其业务含义
- 独立列出“建议后端额外实现的失败场景”表格

f) **前后端契约对齐说明章节**
- 前端承诺（4-6 条）
- 后端承诺（至少包含：响应格式约定、分页格式约定、时间格式约定、ID 格式约定、空值语义约定、slug 格式约定、Token 格式约定，以及**数据库命名与类型映射约定**）
- 此后端承诺中，必须逐条引用步骤1中已精确约定的格式规则，以及 `naming-convention.md` 第6章的转换规则，不得弱化为”建议”或”推荐”
- 所有接口的 Mock 数据（由前端生成）必须与真实后端实现保持结构一致。当后端接口发生任何影响响应结构的变更时，必须同步更新 `blog-frontend/mock-api-doc.md` 并通知前端对应修改 mock handlers。
- 兼容性原则（向前兼容说明）

**步骤14 补充约束：**
- 文档中所有请求体/响应体示例的字段值必须与 `blog-frontend/src/types.ts` 中定义的类型严格一致，不得凭空编造。
- 文档中的字段值示例必须体现步骤1约定的格式规则：ID 必须带正确前缀，日期必须含 `Z` 时区，空字符串字段不可写成 `null`。
- Token 示例必须为有效的 JWT 格式（三段 `.` 分隔的 Base64URL），不得使用 `"mock-jwt-token-abc123"` 等无结构字符串。
- **🔴 后端信息生成（强制）：**
  - 在生成数据模型表格时，必须为每一个字段执行以下步骤：
    1. 根据 `naming-convention.md` 第6.1节，将前端字段名（camelCase）转换为数据库字段名（snake_case）。
    2. 根据 `naming-convention.md` 第6.4节，将前端类型映射为数据库类型（如 `string` → `VARCHAR(255)` 或 `TEXT`）。
    3. 根据 `naming-convention.md` 第6.4节的 VARCHAR vs TEXT 决策规则，确定使用 VARCHAR 还是 TEXT。
    4. 根据字段约束（非空、默认值等）填充数据库约束（如 `NOT NULL DEFAULT ''`）。
    5. 检查转换结果与 `naming-convention.md` 的规则一致性，任何无法确定的映射必须标记为 `⚠️待确认` 并提请人类裁决。
  - 为每个实体标题旁生成数据库表名（由 `naming-convention.md` 第6.2节规则生成）。
- **自检完成后，在对话中输出以下复盘信息（铁律 8）：**

```
✅ 步骤完成清单：
✅步骤0：已执行
✅步骤1：已执行
...
✅步骤14：已执行

📋 步骤1待确认项：[列出或“无”]
⚡ 契约冲突：[列出或“无”]
📄 文档生成模式：[首次创建 / 覆盖更新]
📊 全局覆盖统计：实体 X 个，接口 Y 个
📊 本次变更统计：新增实体 X 个，修改实体 Y 个，删除实体 Z 个（全量覆盖模式下，未变更的已有内容原样保留）
📝 命名规范：[已同步 / 首次生成]
🔍 模糊表述扫描结果：0 处
🔁 重复段落检查：[发现重复并已合并 / 无重复]
🔁 Mock 数据二次校验：[步骤14.5 已执行 / 未执行（首次）]
🗄️ 后端信息生成：数据库字段名/类型/表名已全部基于 naming-convention.md 生成，待确认项 X 个
```

- **将此复盘日志以 HTML 注释形式追加到 `blog-frontend/mock-api-doc.md` 文件末尾：**
  ```
  <!--
  执行复盘：
  步骤完成：0-14 全部完成
  待确认项：无
  契约冲突：无
  生成模式：首次创建
  全局覆盖：实体 9 个，接口 28 个
  命名规范：已同步
  模糊表述扫描：0 处
  重复段落检查：无重复
  后端信息：数据库字段名/类型/表名已生成，待确认0项
  -->
  ```

输出 `blog-frontend/mock-api-doc.md` 完整内容并标注”✅步骤14 完成”。

#### 步骤14.5：基于最终 mock-api-doc 重新生成 Mock Handlers（强制执行）

在步骤14 完成后，必须执行以下操作以保证 Mock 数据与全局契约 100% 一致：

1. **读取** 刚生成/更新的 `blog-frontend/mock-api-doc.md`。
2. **基于文档中的接口定义**（请求/响应结构、错误码、Mock 分支），重新生成 `blog-frontend/src/mocks/handlers.ts`，覆盖步骤4 生成的临时版本。
3. 重新生成的 handlers 必须：
   - 为每个接口实现文档中描述的所有成功和失败分支。
   - 返回的数据结构（字段名、类型、ID 前缀、日期格式等）与文档示例严格一致。
   - 模拟的延迟、报错行为与文档的“Mock 模拟行为说明”匹配。
4. 输出新的 `blog-frontend/src/mocks/handlers.ts` 完整内容，并标注”✅步骤14.5 完成 — Mock Handlers 已与 mock-api-doc.md 同步”。

5. **重新执行受影响步骤**：因 handlers 变更可能影响组件和服务的行为，必须重新执行以下步骤中的交叉校验和测试：
   - 步骤11（组件交叉校验）
   - 步骤12（E2E 测试脚本，若需更新）
   - 步骤13（E2E 测试执行，若环境允许）
   - 若上述步骤出现失败，修复后重新执行直到全部通过。

## 🔍执行后强制复盘

所有步骤执行完毕后，必须在对话中输出复盘日志（内容格式见步骤14末尾），同时确认日志已追加到 `blog-frontend/mock-api-doc.md`。
```