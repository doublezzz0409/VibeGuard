从“按钮失灵、屎山无法修复”走到今天，确实太难了。我们不是在调一个 prompt，而是在**设计一套工程协作系统**。

回顾从起点到现在你亲手推动的关键节点：

---

## 第一阶段：认识问题的本质

你第一次用 MiMo + SuperPows 的体验是：“前端交互失灵，按钮点击无效，后端无法一次启动且 bug 巨多”。你当时就诊断出了根因：**思维懒惰导致无法快速交付**。

这是整个旅程的起点。你没有归咎于“AI 不够强”，而是识别出了**AI 缺少全局约束**这个系统性问题。

---

## 第二阶段：确立流程骨架

我们讨论了 Plan 模式的本质缺陷（单向输出、非协商、缺乏隐形约束），确立了**契约优先、分立开发、E2E 验收**的三段式流程。UI/契约驱动开发成为核心思想——前端根据 UI 图片反馈提示词，在 Mock 上独立开发，前后端通过共享的 TypeScript interface 对齐。

这是从“让 AI 猜”到“给 AI 边界”的转折点。

---

## 第三阶段：发现并修补四个致命盲区

| 盲区 | 发现过程 | 修复位置 |
|------|---------|---------|
| **路由割裂** | 你提交了 `api_miss.md` 报告，3 处链接指向未注册路由，页面空白 | 新增步骤9-10：路由与导航契约清算 |
| **格式假宽容** | `mock-api-doc.md` 中 ID 字段写“格式不限”，slug 写“URL 友好标识” | 步骤1 补充约束 a-i：字段格式精确性强制规则 |
| **增量开发丢全局** | 新功能只输出差异文档，后端无法看到项目完整接口 | 步骤14 新增全局完整性强制规则：首次创建/覆盖更新两种模式 |
| **命名即兴发挥** | `action: "like"` vs `type: "like"` 不一致，缺少命名冲突检测 | 新增 `naming-convention.md`，步骤1 负责维护，步骤14 负责确认 |

---

## 第四阶段：引入需求精炼层

你发现了更深层的问题：模糊需求进入执行层，错误会被杠杆效应放大。然后你设计了 `/translate-requirement`，把它定位成和 `frontend-standard.md` 完全不同的东西：

- `/translate-requirement` 是发散思考者——追问三原则、类比建议、假设反面思维
- `frontend-standard.md` 是机械执行者——铁律禁止优化、禁止思考、禁止合并步骤

两者通过**结构化需求说明**和**共享的 `naming-convention.md`** 无缝衔接。

---

## 第五阶段：消灭行为描述中的实现方案

最后一个反复出现的 bug：需求说明里总冒出 `localStorage`、`useState`、`乐观更新` 这样的实现细节。铁律“禁止”不够，我们加上了**行为描述 vs 实现方案对照表**和**自检规则**（输出前扫描特定技术关键词），才终于杀灭了这个模式。

---

## 你现在拥有的三份 core documents

| 文件 | 角色 | 核心能力 |
|------|------|---------|
| `naming-convention.md` | 命名法律 | 通用原则、实体/字段/API 命名规则、查重目录 |
| `translate-requirement` | 需求分析师 | 加载项目上下文、追问三原则、契约提案、输出结构化需求说明 |
| `frontend-standard.md` | 纪律严明的工程队 | 14 步机械执行、强制交叉校验、全局文档生成、命名规范维护 |

---

## 从“Vibecoding 恐惧”到“可靠交付物”

你说的起点是“按钮失灵、屎山无法修复”。

现在 `/frontend-standard` 的执行链上，每一个可能出错的地方都有一道约束：

- **需求不清**：被 `/translate-requirement` 拦截
- **命名冲突**：被 `naming-convention.md` 查重目录拦截
- **格式模糊**：被步骤1 补充约束 a-i 拦截
- **路由缺失**：被步骤9-10 路由清算拦截
- **字段割裂**：被步骤11 交叉校验拦截
- **文档局部化**：被步骤14 全局完整性强制规则拦截
- **实现方案越界**：被行为描述对照表 + 自检规则拦截

交付物不再是随机的，而是**确定性的**。

---

这套流程是你用一次一次实验、一次一次验收、一次一次“这里不对，再修”打磨出来的。现在可以真正用起来了。